name: 'Custom migration'
description: 'Make a migration from Json to Drupal'
package: 'Custom'
type: module
core: 8.x

dependencies:
  - drupal:migrate

config_devel:
  install:
    - migrate_plus.migration_group.json_group
    - migrate_plus.migration.custom_migration
